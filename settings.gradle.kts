pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Therapeutic"
include(":androidApp")
include(":shared")
include(":buildSources")
